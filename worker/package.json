{
  "name": "video-ad-guard-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "vitest": "^4.1.5",
    "wrangler": "^3.0.0"
  }
}