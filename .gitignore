node_modules/
builds/

dist.crx
dist.pem

*.zip

# Test temp files (but test/ dir itself is tracked)
*.test.*
coverage/

temp/

VideoAdGuard.crx
VideoAdGuard.pem

# Lock files - keep only package-lock.json
yarn.lock
